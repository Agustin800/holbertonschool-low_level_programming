#include <stdio.h>

/**
 * main - do thing
 * Return: 0
 */

int main(void)
{
	char letra;

		for (letra = 'z'; letra >= 'a'; letra--)
	{
		putchar(letra);
	}
	{
			putchar('\n');
	}

		return (0);
}
